{
  "name": "eai-full-stack-exercise",
  "version": "1.0.0",
  "description": "EAI Full Stack Exercise",
  "scripts": {
    "create": "node create-db.js",
    "docker": "docker-compose up",
    "docker:detach": "docker-compose up --detach",
    "start": "npm run docker:detach && echo && npm run create && npm run docker"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/nunoconc/eai-full-stack-exercise.git"
  },
  "dependencies": {
    "pg": "^8.12.0",
    "pg-pool": "^3.6.2"
  },
  "engines": {
    "node": ">=18.3.0"
  }
}
